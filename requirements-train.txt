# Baked into the Modal training image via
# modal.Image.pip_install_from_requirements("requirements-train.txt").
# Consumed only by src/training/train_*.py in Phases 1–3; never installed in
# the production HF Docker image.
#
# Training-only image stays lean: deliberately excludes streamlit,
# scikit-learn, plotly, requests (production-only deps).

torch
numpy
pandas
pyarrow
